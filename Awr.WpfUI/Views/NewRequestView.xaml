<UserControl x:Class="Awr.WpfUI.Views.NewRequestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Awr.WpfUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000" Background="#F0F2F5">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style TargetType="TextBlock" x:Key="FormLabelStyle">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="FormComboStyle">
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox" x:Key="FormInputStyle">
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="AWR REQUEST FORM: SOP-QA-003-F2" FontSize="14" Foreground="#555" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Details -->
        <GroupBox Grid.Row="1" Header="Request Details" FontSize="13" FontWeight="Bold" Margin="0,0,0,20" Background="White" BorderBrush="#CCC">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <!-- Increased width for labels -->
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                    <RowDefinition Height="45"/>
                </Grid.RowDefinitions>

                <!-- Row 1 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource FormLabelStyle}">
                    Request No <Run Text="*" Foreground="Red"/> :
                </TextBlock>
                <TextBox Text="{Binding RequestNo}" IsReadOnly="True" Background="#E9ECEF" Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInputStyle}"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Style="{StaticResource FormLabelStyle}">
                    Type <Run Text="*" Foreground="Red"/> :
                </TextBlock>
                <ComboBox Grid.Row="0" Grid.Column="3" ItemsSource="{Binding AwrTypes}" SelectedItem="{Binding SelectedType}" Style="{StaticResource FormComboStyle}"/>

                <!-- Row 2 -->
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FormLabelStyle}">
                    Material <Run Text="*" Foreground="Red"/> :
                </TextBlock>
                <TextBox Text="{Binding MaterialProduct}" Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInputStyle}"/>

                <TextBlock Grid.Row="1" Grid.Column="2" Style="{StaticResource FormLabelStyle}">
                    Batch <Run Text="*" Foreground="Red"/> :
                </TextBlock>
                <TextBox Text="{Binding BatchNo}" Grid.Row="1" Grid.Column="3" Style="{StaticResource FormInputStyle}"/>

                <!-- Row 3 -->
                <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource FormLabelStyle}">
                    AR No <Run Text="*" Foreground="Red"/> :
                </TextBlock>
                <TextBox Text="{Binding ArNo}" Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInputStyle}"/>

                <TextBlock Grid.Row="2" Grid.Column="2" Style="{StaticResource FormLabelStyle}">
                    AWR No <Run Text="*" Foreground="Red"/> :
                </TextBlock>

                <!-- SEARCHABLE COMBOBOX FIX -->
                <ComboBox Grid.Row="2" Grid.Column="3" 
                          ItemsSource="{Binding FilteredAwrNumbers}" 
                          Text="{Binding AwrNo, UpdateSourceTrigger=PropertyChanged}"
                          IsEditable="True"
                          IsTextSearchEnabled="False"
                          StaysOpenOnEdit="True"
                          Style="{StaticResource FormComboStyle}"/>

                <!-- Row 4: Qty -->
                <TextBlock Text="Qty Required:" Grid.Row="3" Grid.Column="0" Style="{StaticResource FormLabelStyle}"/>

                <Grid Grid.Row="3" Grid.Column="1" Height="30" Margin="0,0,20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Text="{Binding QtyRequired}" IsEnabled="{Binding IsQtyEnabled}" 
                             Style="{StaticResource FormInputStyle}" Margin="0" 
                             HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="0"/>

                    <Grid Grid.Column="1" Margin="2,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Button Content="▲" FontSize="8" Padding="0" Margin="0,0,0,1" Command="{Binding IncrementQtyCommand}" IsEnabled="{Binding IsQtyEnabled}"/>
                        <Button Content="▼" FontSize="8" Padding="0" Margin="0,1,0,0" Grid.Row="1" Command="{Binding DecrementQtyCommand}" IsEnabled="{Binding IsQtyEnabled}"/>
                    </Grid>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Comments -->
        <GroupBox Grid.Row="2" Header="Comments / Justification" FontSize="13" FontWeight="Bold" Margin="0,0,0,20" Background="White" BorderBrush="#CCC">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="* Required" Foreground="Red" FontSize="10" Margin="5,0,0,0"/>
                <TextBox Grid.Row="1" Text="{Binding Comments}" Style="{StaticResource FormInputStyle}" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" Height="NaN" Margin="5,10,5,10"/>
            </Grid>
        </GroupBox>

        <!-- Footer -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Status: " FontWeight="Bold" Foreground="#333"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#333"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ProgressBar Width="100" Height="15" IsIndeterminate="True" Margin="0,0,15,0" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                <Button Content="Submit Request" Command="{Binding SubmitCommand}" Background="#28A745" Foreground="White" FontWeight="Bold" FontSize="13" Width="150" Height="35" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
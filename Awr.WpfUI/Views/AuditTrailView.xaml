<UserControl x:Class="Awr.WpfUI.Views.AuditTrailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="White">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="System Audit Trail" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="0,0,20,0"/>
                <CheckBox Content="Show All Records" IsChecked="{Binding ShowAll}" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBox Width="200" Height="30" Margin="0,0,10,0" Padding="5" VerticalContentAlignment="Center" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         ToolTip="Filter..."/>
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="15,5"/>
            </StackPanel>
        </Grid>

        <!-- Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  BorderBrush="#DDD" BorderThickness="1" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Request No" Binding="{Binding RequestNo}" Width="140"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" FontWeight="Bold"/>
                <DataGridTextColumn Header="Product" Binding="{Binding MaterialProduct}" Width="200"/>
                <DataGridTextColumn Header="Requester" Binding="{Binding RequestedBy}" Width="100"/>
                <DataGridTextColumn Header="Approver" Binding="{Binding IssuedBy}" Width="100"/>
                <DataGridTextColumn Header="Receiver" Binding="{Binding ReceivedBy}" Width="100"/>
                <DataGridTextColumn Header="Remark" Binding="{Binding Remark}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#666"/>
    </Grid>
</UserControl>
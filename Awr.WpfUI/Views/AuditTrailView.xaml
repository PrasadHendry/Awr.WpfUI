<UserControl x:Class="Awr.WpfUI.Views.AuditTrailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Awr.WpfUI.ViewModels"
             xmlns:conv="clr-namespace:Awr.WpfUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200" Background="#F0F2F5">

    <UserControl.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBoolToBool"/>

        <!-- 1. TEXT WRAPPING STYLE FOR COLUMNS -->
        <Style x:Key="WrappedColumnStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2,5"/>
        </Style>

        <!-- INPUT STYLES -->
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox" x:Key="FilterInputStyle">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0,0,0"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="FilterComboStyle">
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- HEADER STYLE -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#41546E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,12"/>
            <!-- Taller Header -->
            <Setter Property="BorderBrush" Value="#667D97"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filters -->
            <RowDefinition Height="*"/>
            <!-- Grid -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- 1. FILTERS SECTION -->
        <GroupBox Header="Filters" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" Background="White" BorderBrush="#CCC">
            <Grid Margin="10,10,10,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Request No:" Grid.Row="0" Grid.Column="0" Style="{StaticResource LabelStyle}"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Style="{StaticResource FilterInputStyle}"/>
                    <TextBlock Text="AWR Type:" Grid.Row="0" Grid.Column="2" Style="{StaticResource LabelStyle}"/>
                    <ComboBox Grid.Row="0" Grid.Column="3" ItemsSource="{Binding AwrTypes}" SelectedItem="{Binding SelectedType}" Style="{StaticResource FilterComboStyle}"/>
                    <TextBlock Text="Status:" Grid.Row="0" Grid.Column="4" Style="{StaticResource LabelStyle}"/>
                    <ComboBox Grid.Row="0" Grid.Column="5" ItemsSource="{Binding Statuses}" SelectedItem="{Binding SelectedStatus}" Style="{StaticResource FilterComboStyle}"/>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="3" Margin="0,10,0,0">
                        <CheckBox Content="My Requests Only" IsChecked="{Binding ShowAll, Converter={StaticResource InverseBoolToBool}}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <CheckBox Content="Show All" IsChecked="{Binding ShowAll}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Grid.Column="5" Text="{Binding Items.Count, StringFormat='Total Records: {0}'}" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" Margin="0,10,15,0"/>
                </Grid>
                <Button Grid.Column="1" Content="Search / Refresh" Command="{Binding RefreshCommand}" Background="#007BFF" Foreground="White" FontWeight="Bold" FontSize="14" Margin="10,0,0,0" Height="40" VerticalAlignment="Top" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </GroupBox>

        <!-- 2. DATA GRID SECTION -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" 
                  ScrollViewer.HorizontalScrollBarVisibility="Visible"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  GridLinesVisibility="All"
                  VerticalGridLinesBrush="#EEE"
                  HorizontalGridLinesBrush="#EEE"
                  BorderBrush="#555" BorderThickness="1"
                  Background="White"
                  RowHeight="40" 
                  FrozenColumnCount="2">
            <!-- ROW HEIGHT INCREASED FOR WRAPPING -->

            <DataGrid.Columns>
                <!-- Frozen Columns -->
                <DataGridTextColumn Header="Request No." Binding="{Binding RequestNo}" Width="140" FontWeight="SemiBold" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="AWR No." Binding="{Binding AwrNo}" Width="140" ElementStyle="{StaticResource WrappedColumnStyle}"/>

                <!-- Scrollable Columns -->
                <DataGridTextColumn Header="Type" Binding="{Binding AwrType}" Width="80" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="Material/Product" Binding="{Binding MaterialProduct}" Width="250" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="Batch No." Binding="{Binding BatchNo}" Width="120" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="AR No." Binding="{Binding ArNo}" Width="120" ElementStyle="{StaticResource WrappedColumnStyle}"/>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource WrappedColumnStyle}">
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Prepared By (QC)" Binding="{Binding RequestedBy}" Width="130" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="Approved By (QA)" Binding="{Binding IssuedBy}" Width="130" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="Printed By (QC)" Binding="{Binding ReceivedBy}" Width="130" ElementStyle="{StaticResource WrappedColumnStyle}"/>
                <DataGridTextColumn Header="Voided By (QC)" Binding="{Binding ReturnedBy}" Width="130" ElementStyle="{StaticResource WrappedColumnStyle}"/>

                <!-- Large Fixed Width for Remark -->
                <DataGridTextColumn Header="Remark / Justification" Binding="{Binding Remark}" Width="400" ElementStyle="{StaticResource WrappedColumnStyle}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 3. FOOTER -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,0">
            <Button Content="Export CSV" Width="120" Height="30" Margin="0,0,10,0" Background="#6C757D" Foreground="White" FontWeight="SemiBold" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Content="Export Excel" Width="120" Height="30" Background="#6C757D" Foreground="White" FontWeight="SemiBold" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
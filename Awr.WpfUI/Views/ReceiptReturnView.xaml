<UserControl x:Class="Awr.WpfUI.Views.ReceiptReturnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="White">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Pending Print &amp; Receipt" FontSize="18" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="15,5"/>
            </StackPanel>
        </Grid>

        <!-- Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  BorderBrush="#DDD" BorderThickness="1" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Request No" Binding="{Binding RequestNo}" Width="140" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="AWR No" Binding="{Binding AwrNo}" Width="140"/>
                <DataGridTextColumn Header="Product" Binding="{Binding MaterialProduct}" Width="*"/>
                <DataGridTextColumn Header="Batch" Binding="{Binding BatchNo}" Width="100"/>
                <DataGridTextColumn Header="Approved By" Binding="{Binding IssuedBy}" Width="120"/>
                <DataGridTextColumn Header="Date" Binding="{Binding IssuedAt, StringFormat=dd-MM-yyyy}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Actions -->
        <Border Grid.Row="2" Margin="0,15,0,0" Padding="15" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Selected Request" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding SelectedItem.MaterialProduct}" Foreground="#555" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0">
                    <TextBlock Text="Void Reason (Required to Cancel)" FontSize="12" Foreground="#666" Margin="0,0,0,3"/>
                    <TextBox Text="{Binding VoidReason, UpdateSourceTrigger=PropertyChanged}" Height="40" Padding="5"/>
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Orientation="Horizontal">
                    <ProgressBar Width="80" Height="15" IsIndeterminate="True" Margin="0,0,10,0" 
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>

                    <Button Content="Cancel / Void" Command="{Binding VoidCommand}" 
                            Background="#DC3545" Foreground="White" FontWeight="Bold" 
                            Height="35" Width="100" Margin="0,0,10,0" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="Print &amp; Confirm" Command="{Binding PrintCommand}" 
                            Background="#28A745" Foreground="White" FontWeight="Bold" 
                            Height="35" Width="140" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>